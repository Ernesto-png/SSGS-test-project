module.exports = {
  collectCoverage: true, // Attiva la raccolta del coverage
  coverageDirectory: 'coverage', // Directory per i report
  coverageReporters: ['text', 'lcov'], // Report in formato testo e HTML
};
